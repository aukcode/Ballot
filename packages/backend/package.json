{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for my apps",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:ts",
    "build:ts": "tsc",
    "serve": "node build/Main.js",
    "start": "npm run build && npm run serve"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "typescript": "3.5.3"
  },
  "author": "auk",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
